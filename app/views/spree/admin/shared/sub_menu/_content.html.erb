<ul id="sidebar-content" class="collapse nav nav-pills nav-stacked">
  <%= tab :feed_items, :match_path => /feed_items/ %>
  <%= tab :surf_club_images, :match_path => /surf_club_images/ %>
  <%= tab :lookbooks, :match_path => /(slide_images)|(lookbooks)/ %>
  <%= tab :posts, :match_path => '/posts' %>
  <%= tab :press_items, :match_path => '/press_items' %>
  <%= tab :art, :match_path => '/art' %>
  <%= tab :stockists, :match_path => '/stockists' %>
  <%= tab :about, :match_path => '/about' %>
  <%= tab :returns_exchanges, :match_path => '/returns_exchanges' %>
</ul>
